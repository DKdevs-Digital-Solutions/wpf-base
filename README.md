# Proxy Backend (sem WhatsApp Flows)

Este projeto contém apenas o backend de **Proxy** (rotas `/proxy/*`, `/status/*`, `/docs`, `/openapi.json`) protegido por **x-api-key**.

## Rodar local
```bash
npm install
npm start
```

## Docker Compose
```bash
docker compose up --build -d
```
